# Screenshots for the README

Drop the step screenshots here. The README references these file names:

| File | Shows |
|---|---|
| `step1-openiv-scriptrpf.png` | OpenIV opened at `update.rpf` (or `update2.rpf` on newer versions) `/x64/levels/gta5/script/script.rpf` |
| `step1-openiv-export.png`    | "Select all" + right-click → "Extract…" in OpenIV |
| `step2-decompiler.png`       | The script decompiler running with the native tables |

PNG or JPG is fine; keep the same file names (or update the paths in `../../README.md`).
